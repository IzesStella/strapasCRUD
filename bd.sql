CREATE TABLE `sistema`.`filmes` (
  `id` INT NOT NULL AUTO_INCREMENT,
  `nome` VARCHAR(45) NOT NULL,
  `diretor` VARCHAR(45) NOT NULL,
  `duracao` VARCHAR(45) NOT NULL,
  `protagonista` VARCHAR(45) NOT NULL,
  PRIMARY KEY (`id`));